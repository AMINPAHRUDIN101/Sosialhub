# Sosialhub
mv socialhub_app.html index.html git init git add index.html git commit -m "Initial SocialHub site" git branch -M main # jika sudah ter-login dengan GitHub CLI: gh repo create AMINPAHRUDIN101/socialhub --public --source=. --remote=origin --push
